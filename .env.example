# Database Connection Settings (matches docker-compose.yml by default)
DB_HOST=db
DB_USER=erasgames_user
DB_PASSWORD=choose_a_strong_password
DB_DATABASE=erasgames
DB_PORT=3306

# Allowed Hosts for Proxy Routing
ALLOWED_HOSTS=localhost,erasgames.com
